<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml" />
    <include resource="org/springframework/boot/logging/logback/console-appender.xml" />

    <!-- Configurable root log folder — set per profile in application-{profile}.yml -->
    <springProperty scope="context" name="LOG_PATH" source="logging.log-path" defaultValue="logs"/>

    <springProperty scope="context" name="EARNINGS_LOG_FILE" source="logging.cronjob.files.earnings" defaultValue="${LOG_PATH}/earnings.log" />
    <springProperty scope="context" name="DAY_STOCK_MOVEMENT_DATA_LOG_FILE" source="logging.cronjob.files.dayStockMovementData" defaultValue="${LOG_PATH}/day-stock-movement-data.log" />
    <springProperty scope="context" name="DAY_STOCK_MOVEMENT_CLEANUP_LOG_FILE" source="logging.cronjob.files.dayStockMovementCleanup" defaultValue="${LOG_PATH}/day-stock-movement-cleanup.log" />
    <springProperty scope="context" name="DAY_STOCK_MOVEMENT_KEY_MAP_ENTRY_LOG_FILE" source="logging.cronjob.files.dayStockMovementKeyMapEntry" defaultValue="${LOG_PATH}/day-stock-movement-key-map-entry.log" />
    <springProperty scope="context" name="WEEKLY_UPCOMING_EARNINGS_LOG_FILE" source="logging.cronjob.files.weeklyUpcomingEarning" defaultValue="${LOG_PATH}/weekly-upcoming-earnings.log" />
    <springProperty scope="context" name="DAILY_ANALYST_RATING_LOG_FILE" source="logging.cronjob.files.dailyAnalystRating" defaultValue="${LOG_PATH}/daily-analyst-rating.log" />

    <appender name="EARNINGS_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${EARNINGS_LOG_FILE}</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${EARNINGS_LOG_FILE}.%d{yyyy-MM-dd}.%i.gz</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>14</maxHistory>
            <totalSizeCap>200MB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <appender name="DAY_STOCK_MOVEMENT_DATA_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${DAY_STOCK_MOVEMENT_DATA_LOG_FILE}</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${DAY_STOCK_MOVEMENT_DATA_LOG_FILE}.%d{yyyy-MM-dd}.%i.gz</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>14</maxHistory>
            <totalSizeCap>200MB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <appender name="DAY_STOCK_MOVEMENT_CLEANUP_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${DAY_STOCK_MOVEMENT_CLEANUP_LOG_FILE}</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${DAY_STOCK_MOVEMENT_CLEANUP_LOG_FILE}.%d{yyyy-MM-dd}.%i.gz</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>14</maxHistory>
            <totalSizeCap>200MB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <appender name="DAY_STOCK_MOVEMENT_KEY_MAP_ENTRY_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${DAY_STOCK_MOVEMENT_KEY_MAP_ENTRY_LOG_FILE}</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${DAY_STOCK_MOVEMENT_KEY_MAP_ENTRY_LOG_FILE}.%d{yyyy-MM-dd}.%i.gz</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>14</maxHistory>
            <totalSizeCap>200MB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <appender name="WEEKLY_UPCOMING_EARNINGS_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${WEEKLY_UPCOMING_EARNINGS_LOG_FILE}</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${WEEKLY_UPCOMING_EARNINGS_LOG_FILE}.%d{yyyy-MM-dd}.%i.gz</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>14</maxHistory>
            <totalSizeCap>200MB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <appender name="DAILY_ANALYST_RATING_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${DAILY_ANALYST_RATING_LOG_FILE}</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${DAILY_ANALYST_RATING_LOG_FILE}.%d{yyyy-MM-dd}.%i.gz</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>14</maxHistory>
            <totalSizeCap>200MB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <springProfile name="cronjob">
        <logger name="com.rama.mudstock.scheduler.EarningsDateScheduler" level="INFO" additivity="false">
            <appender-ref ref="EARNINGS_FILE" />
            <appender-ref ref="CONSOLE" />
        </logger>

        <logger name="com.rama.mudstock.scheduler.DayStockMovementScheduler" level="INFO" additivity="false">
            <appender-ref ref="DAY_STOCK_MOVEMENT_DATA_FILE" />
            <appender-ref ref="CONSOLE" />
        </logger>

        <logger name="com.rama.mudstock.scheduler.DayStockMovementCleanupScheduler" level="INFO" additivity="false">
            <appender-ref ref="DAY_STOCK_MOVEMENT_CLEANUP_FILE" />
            <appender-ref ref="CONSOLE" />
        </logger>

        <logger name="com.rama.mudstock.scheduler.DayStockMovementKeyMapEntryScheduler" level="INFO" additivity="false">
            <appender-ref ref="DAY_STOCK_MOVEMENT_KEY_MAP_ENTRY_FILE" />
            <appender-ref ref="CONSOLE" />
        </logger>

        <logger name="com.rama.mudstock.scheduler.WeeklyUpcomingEarningCronjob" level="INFO" additivity="false">
            <appender-ref ref="WEEKLY_UPCOMING_EARNINGS_FILE" />
            <appender-ref ref="CONSOLE" />
        </logger>

        <logger name="com.rama.mudstock.scheduler.analyst.DailyAnalystRatingUpdateCronjob" level="INFO" additivity="false">
            <appender-ref ref="DAILY_ANALYST_RATING_FILE" />
            <appender-ref ref="CONSOLE" />
        </logger>

        <logger name="com.rama.mudstock.facade.AnalystRatingFacade" level="INFO" additivity="false">
            <appender-ref ref="DAILY_ANALYST_RATING_FILE" />
            <appender-ref ref="CONSOLE" />
        </logger>

        <root level="INFO">
            <appender-ref ref="CONSOLE" />
        </root>
    </springProfile>

    <springProfile name="!cronjob">
        <root level="INFO">
            <appender-ref ref="CONSOLE" />
        </root>
    </springProfile>
</configuration>